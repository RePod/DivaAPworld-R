**./early/** for most diffs or the bare minimum needed to patch the base world to a state for multiple diffs.

**./late/** if diffs are too great and can instead be diffed (manually) from the applied early diffs. Rarely used.
